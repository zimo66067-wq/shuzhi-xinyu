# ============================================
# 数智心屿 · 前端环境变量示例
# 使用方法：
#   1. 复制本文件为 .env：  cp .env.example .env
#   2. 修改下面的值（如需要）
#   3. .env 已在 .gitignore，不会被提交
#
# 注意：Vite 规则下 VITE_ 前缀变量会被打包进 JS bundle 公开可见。
#       任何"密钥"类信息绝不要放在这里！家长密码已改为后端校验。
# ============================================

# ---- 可选：后端 API 地址（部署时使用） ----
# 开发环境用 vite proxy 时留空（请求走相对路径 → vite proxy → localhost:5000）
# 生产环境同源部署（nginx 反代）也留空
# 仅当前端和后端不同源时填写，例如：
# VITE_API_BASE=https://api.xinyu.example.com
#
# ⚠️ 生产环境必须用 HTTPS，否则浏览器禁用麦克风（STT 失效）

# （此文件目前没有必填项，仅作为占位）
